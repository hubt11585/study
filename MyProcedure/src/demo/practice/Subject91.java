package demo.practice;

/**
 * 给定一个字符串(s) 和一个字符模式(p) ，实现一个支持'?'和'*'的通配符匹配。
 * '?' 可以匹配任何单个字符。
 * '*' 可以匹配任意字符串（包括空字符串）。
 * 两个字符串完全匹配才算匹配成功。
 */
public class Subject91 {
    public static void main(String[] args) {
        String str1 = "a";
        String str2 = "a";
        new Subject91().isMatch(str1,str2);
    }

    public boolean isMatch(String s, String p) {
        return false;
    }
}
